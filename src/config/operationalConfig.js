// Mirrors the Doctor Dashboard's operationalConfig.js -- same weekly
// cadence, kept as its own named constant rather than a magic `7` so the
// two apps can never silently drift out of sync on what "weekly" means.
export const ASSESSMENT_INTERVAL_DAYS = 7;

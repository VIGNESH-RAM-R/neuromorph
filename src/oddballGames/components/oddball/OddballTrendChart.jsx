// This component now lives at src/components/shared/TrendChart.jsx (used by
// other Neuromorph modules too). Re-exported here so existing imports in
// the Oddball module keep working unchanged.
export { default } from '../shared/TrendChart';
